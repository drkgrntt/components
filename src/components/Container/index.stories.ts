import { Container } from ".";

const Template = ({}) => `<drk-container>content</drk-container>`;

export default {
  component: Container,
  title: "Library/Components/Container",
};

export const Default = Template.bind({});
Default.args = {};
